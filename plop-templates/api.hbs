import request from '@/plugins/axios'
{{#each layout}}
{{#each children}}

{{#if key}}
/**
 * {{name}}
 */
export const I{{toUpperCaseHump key}}: any = (param: object) => {
    return request.post({ url: '', param: param }, async (res: any) => {
        const dist: any = {{{stringify copyData}}}

        return dist
    })
}
{{/if}}
{{/each}}
{{/each}}