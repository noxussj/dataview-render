<template>
    <!-- {{name}} -->
    <app-card title="{{name}}">
        <div class="{{modelClassName parent.prefix}}{{parent.key}}__{{key}}">
            <barx-multiple-line :bar-width="barWidth" :color="color" :data="data" :opt="opt" show-background />
        </div>
    </app-card>
</template>

<script lang="ts" setup>
import { I{{toUpperCaseHump key}} } from '@/api/{{parent.prefix}}{{parent.key}}/index';

const data = await I{{toUpperCaseHump key}}()

/**
 * 图表配置
 */
const barWidth = 20
const color = []
const opt = {
    legend: {
        left: 'center'
    },
    yAxis: [{ name: '单位：人数' }, { name: '佩戴比例：%' }]
}
</script>

<style lang="scss" scoped>
.{{modelClassName parent.prefix}}{{parent.key}}__{{key}} {
    height: 100%
}
</style>